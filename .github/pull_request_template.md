## What changed

<!-- One short paragraph: what changed and why. -->

## Checklist

- [ ] `npm run verify` is green (mirrors + i18n + build)
- [ ] `npm run versions:check` is green (or `versions:update` ran in this PR as a release cut)
- [ ] Docs updated where behavior changed (README / docs/**)
- [ ] No secrets, tokens, or personal data included
- [ ] Locale changes: the `en` source was updated first and the overrides pipeline was used

## Notes for reviewers

<!-- Screenshots for UI changes (no personal data). Reproduction steps for fixes. -->
