{
  "name": "x-block-all-tweets-from-that-country",
  "version": "0.1.11",
  "private": true,
  "description": "X - Block all tweets from that country or region (Premium11 Chromium extension).",
  "scripts": {
    "build": "node scripts/build.mjs",
    "i18n:check": "node scripts/check-i18n.mjs",
    "i18n:scaffold": "node scripts/scaffold-locale.mjs",
    "agents:sync": "node scripts/agents-sync.mjs",
    "agents:verify": "node scripts/agents-verify.mjs",
    "verify": "npm run agents:verify && npm run i18n:check && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}
